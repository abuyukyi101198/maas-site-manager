import { ContentSection } from "@canonical/maas-react-components";

const ImageServer = () => {
  return (
    <ContentSection>
      <ContentSection.Title>Image server</ContentSection.Title>
    </ContentSection>
  );
};

export default ImageServer;
